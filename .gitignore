__pycache__/
*.pyc
build.log
hud-build.log
hud-v2-build.log
build/
target/
runs.jsonl
hud/dist/
hud/gen/
hud/icons/icon.iconset/
*.log

# Committed test fixtures (deliberate, not transient logs)
!tests/fixtures/worker-logs/*.log
.venv/

# Machine-specific operating brief — PII-bearing, gitignored per fleet security rule.
# Sanitised template is committed as CLAUDE.example.md (copy it to CLAUDE.md locally).
CLAUDE.md

# Operator PII scan terms (never commit — real PII)
.pii-scan-terms.txt
